# Slider 0.2.2

- Replace the custom slide image field with the shared HiddenCMS media picker.
- Provide the standard image preview, media library navigation and upload experience.
- Remove obsolete Slider-specific image picker markup and JavaScript.

Compatibility: HiddenCMS Core ^0.8 || ^0.9, PHP >=8.1. No database migration.

Validation: PHP syntax, Slider settings, database behavior and administration rendering verified locally.
